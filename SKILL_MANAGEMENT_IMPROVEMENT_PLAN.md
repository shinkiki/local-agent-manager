# 스킬 관리 개선 계획 (스킬공유·편집·삭제·휴지통·AIA)

- 상태: 구현됨 (2026-08-21: 출처 메타·프로젝트 위치 배포·다중 위치 매트릭스·동기화(수동/자동)·보관 원본 편집기·AIA 위임 편집까지 완료)
- 작성일: 2026-08-20

> 2026-08-20 개념 개정: 탭은 스킬정보/스킬관리, "공유"는 "보관"으로 전환했다.
> 스킬보관 = 보관 저장소(~/.agents/skills)로 원본 승격(기존 설치본 유지),
> 에이전트 적용 = 카드의 "사용" 체크박스(체크=배포, 해제=즉시 휴지통 이동, 확인 없음),
> 삭제 = 보관 스킬만 대상(원본+모든 사용본, 프로젝트 저장소 포함, 한 그룹).
> 백업/복구는 스킬관리로 이동해 보관 저장소를 대상으로 한다. 아래 본문 용어 중
> "공유/배포"는 이 개정 전 표기다.
- 대상: Agent Manager Tauri
- 지원 에이전트: Claude Code, OpenAI Codex. Antigravity는 사용자 스킬 루트가 없어 조회 전용.

## 1. 용어 정의 (확정)

사용자 노출 문자열만 교체한다. 내부 코드 식별자(`SkillLibrary`, `publish_common_skill` 등)는 유지한다.

| 기존 | 확정 용어 | 비고 |
| --- | --- | --- |
| 통합 관리 탭 | **스킬관리** | "통합 스킬"이라는 별도 명사를 만들지 않는다. 공유 여부는 스킬의 속성이다 |
| plugin/system/builtin 그룹 | **내장 스킬** | 별도 탭, 조회 전용. 내부 배지는 `플러그인` / `에이전트 기본` |
| 게시(publish) | **배포** | 대상은 외부가 아니라 내 에이전트 |
| 공급자(provider) | **에이전트** | Claude / Codex / Antigravity |
| 통합으로 승격(import) | **스킬공유 ON** | 토글 |
| (신규) 공유 해제 | **스킬공유 OFF** | 아래 2절. 파일 보존 전환으로 확정 |
| 공통 원본 | **원본** (저장 위치는 "공유 저장소" `~/.agents/skills`) | |
| 링크/사본 | UI 기본 노출 제거, 상세정보에만 표시 | 동작 차이가 없으므로 구현 세부사항 |
| 일부 게시 / 미게시 / 최신 | 상태 배지 폐지 | 배포 여부는 에이전트 매트릭스가 표현 |
| 갈라짐(diverged) | **외부 수정 감지** | 유일한 조치 필요 배지 |
| (신규) | **스킬공유** 배지 | 공유 중임을 표시. 경고 아님 |

사용자 설명 네 문장: **"공유 켠 스킬을 고치면 배포된 모든 에이전트에 반영된다. 공유 끈 스킬은 그 에이전트에만 있다. 내장 스킬은 못 고친다. 지운 건 휴지통에 있다."**

## 2. 공유 모델

- **스킬공유 ON**: 에이전트 스킬의 원본을 공유 저장소(`~/.agents/skills/<key>`)로 올리고 해당 에이전트 연결을 유지한다. 기존 `import_skill_to_common` + `publish_common_skill` 재사용.
- **스킬공유 OFF (확정)**: 원본을 공유 저장소에서 내리되, 현재 배포된 각 에이전트에는 **독립 사본으로 보존**한다(에이전트 스킬로 전환, 파일 보존). 이후 편집은 각 에이전트에만 반영된다. ON의 역연산이며 파괴적이지 않다.
- **에이전트별 배포**: 공유 스킬은 에이전트 매트릭스 토글로 배포를 켜고 끈다. 토글 OFF = 그 에이전트에서 확정 삭제(휴지통행, 4절).
- 공유 여부 판별은 scope가 아니라 **공유 저장소에 같은 키가 존재하는지**로 한다. personal/project 위치와 무관하게 키가 있으면 공유 스킬의 배포본이다.

## 3. 편집 규칙

| 대상 | 동작 | 반영 범위 |
| --- | --- | --- |
| 공유 스킬 (링크/사본 무관) | 원본 수정 → 배포된 모든 에이전트에 자동 재배포 | 전체 전파 (의도된 동작) |
| 에이전트 스킬 (비공유 personal/project) | 제자리 편집 | 해당 에이전트/프로젝트만 |
| 내장 스킬 (플러그인/에이전트 기본) | 편집·삭제 금지 | 코어에서 거부 (UI 버튼 숨김만으로 부족) |

- 편집 대상은 **SKILL.md + 부속 파일 전체**(scripts, references, assets 등). 텍스트 파일 내용 편집과 파일 추가·삭제·이름변경을 지원한다. 바이너리 파일은 교체·삭제만 지원하고 내용 편집은 제외한다. SKILL.md 단일 파일로 제한하면 스킬을 실질적으로 만질 수 없다는 지적을 반영한 범위다.
- 저장 시 검증: 기존 한도 재사용(SKILL.md 5MB, 파일 2,000개, 총 64MB), frontmatter name/description 규칙(`validate_skill_key`), 스킬 내부 심볼릭 링크 금지(기존 배포 검증과 동일), 경로 탈출 방지.
- **낙관적 잠금 필수**: `put_doc`의 `expectedModifiedAt` 패턴 재사용. UI와 AIA가 동시에 같은 스킬을 편집할 수 있다.
- 저장 전 반영 범위 고지: 공유 스킬이면 "저장 시 N개 에이전트에 반영됩니다"를 UI·AIA 응답 모두에 표시한다.
- **외부 수정 감지 해소**: 배포본이 외부 도구로 수정되어 원본과 달라진 경우 두 가지 액션을 제공한다 — `원본으로 되돌리기`(재배포, 기존 배포본은 휴지통 경유) / `이 버전을 원본에 반영`(기존 import 재사용).

## 4. 삭제 규칙과 상태 해석 변경

삭제는 철회(재배포 대기)가 아니라 **확정 삭제**다. "그 에이전트에서는 필요 없음"을 뜻한다.

| 삭제 위치 | 동작 |
| --- | --- |
| 에이전트 위치의 배포본 (매트릭스 토글 OFF 포함) | 그 에이전트에서 삭제 → 휴지통. 원본과 다른 에이전트 배포본은 유지 |
| 공유 스킬 자체 (스킬관리에서 삭제) | 원본 + 모든 배포본을 **한 휴지통 그룹**으로 함께 삭제. 배포본 먼저, 원본 마지막(링크가 깨진 채 남지 않도록). 복구도 그룹 단위 |
| 에이전트 스킬 (비공유) | 해당 위치에서 삭제 → 휴지통 |
| 내장 스킬 | 삭제 금지 |

공유 스킬 삭제(전부 제거)와 스킬공유 OFF(파일 보존 전환)는 별개 동작이다.

**동기화 상태 해석 변경 (필수 선행)**: 현재 로직은 미배포 에이전트를 `missing = 조치 필요`로 계산해 "일부 게시" 배지와 기본 배포 대상 자동 선택을 만든다. 확정 의미와 충돌하므로 다음을 함께 변경한다.

- `defaultPublishTargets`: `missing` 제외, `divergent`(외부 수정 감지)만 기본 선택. 재배포는 매트릭스에서 명시적으로 켠다.
- `skillSyncState`: "배포 가능한 모든 곳에 있어야 최신" → "**배포된 곳들이 모두 최신이면 정상**". 미배포 에이전트는 경고가 아닌 정보(빈 토글).
- 영속 제외 목록은 도입하지 않는다. 무상태 스캐너 설계를 유지하고 해석만 바꾼다.

## 5. 휴지통

주체(사용자 UI / AIA MCP)와 대상(에이전트 스킬 / 배포본 / 원본) 구분 없이 단일 규칙.

- 위치: 앱 데이터 디렉터리 `trash/skills/<삭제시각>-<key>/`
- 항목별 manifest: 원래 절대경로, scope, 에이전트, 공유 여부, 삭제 주체(user/aia), 콘텐츠 해시, 항목 종류(`directory` | `link`), 그룹 ID(공유 스킬 일괄 삭제용)
- **심볼릭 링크는 링크 자체만 기록**하고 복구 시 링크를 재생성한다. 링크 대상 디렉터리(원본)를 옮기지 않는다.
- 재배포 시 덮어쓰는 기존 배포본도 휴지통을 경유한다(현행 `BACKUP_PREFIX` 임시 백업을 휴지통으로 승격).
- 복구: 원래 경로로 원상복원. 경로에 새 스킬이 생겼으면 실패를 반환하고 덮어쓰지 않는다. 복구되면 다음 스캔에서 자동으로 배포 상태에 다시 잡힌다(별도 재등록 절차 없음).
- 보존 정책: 자동 만료 없이 **수동 비우기 + 용량 표시**로 시작(스케줄러 의존성 배제).
- Windows: 볼륨 간 rename 실패 대비 copy+delete 폴백.

## 6. 화면 구조

```
[스킬관리 탭]
  스킬 카드: 이름 · [스킬공유 ●ON/○OFF] · 에이전트 매트릭스(Claude ✓ / Codex ✓ / AG 미지원)
             공유 OFF면 위치 배지: "Claude · 개인" / "프로젝트 <이름>"
  배지:      스킬공유(공유 중 표시) / 외부 수정 감지(조치 필요)
  액션:      편집 · 삭제 · (공유 ON일 때) 에이전트별 배포 토글

[내장 스킬 탭]  조회 전용. 배지: 플러그인 / 에이전트 기본
```

- "개인/프로젝트"는 1차 분류가 아니라 에이전트 스킬의 위치 표시로만 사용한다.
- 배포 0곳인 공유 스킬은 배지 대신 빈 매트릭스로 표현한다(필요시 "배포된 에이전트 없음" 보조 문구).
- 상태 배지는 위 2종 외에 두지 않는다.

## 7. AIA MCP

`system_mcp.rs`의 `capability!` 목록에 추가한다. UI와 **동일한 코어 함수**를 호출하고, Execute는 기존 감사 로그가 자동으로 감싼다. 설명문과 응답 필드는 1절 용어를 사용한다.

| 작업 | 종류 | 동작 |
| --- | --- | --- |
| `get_skill_library` | Read | 스킬 목록·공유 여부·에이전트별 배포 상태·해석된 경로 |
| `get_common_skill_detail` | Read | 공유 원본 상세 |
| `check_skill_delete` | Read | 삭제 영향 보고(링크 여부, 공유 여부, 파일 수, 그룹 범위) |
| `list_skill_trash` | Read | 휴지통 목록 + manifest |
| `update_skill` | Execute | 공유 스킬이면 원본 수정+전체 재배포(배포 영수증 반환), 비공유면 제자리 수정. `expectedModifiedAt` 필수 |
| `delete_skill` | Execute | 휴지통 이동. `confirm: true` 필수, `check_skill_delete` 선행 강제 |
| `restore_skill_trash` | Execute | 원경로 복구. 충돌 시 실패 반환 |
| `set_skill_shared` | Execute | 공유 ON(승격+배포) / OFF(파일 보존 전환) |
| `set_skill_agent_deployment` | Execute | 에이전트 배포 켜기(재배포) / 끄기(그 에이전트에서 삭제→휴지통) |

- `update_skill` 응답에 반영 범위를 명시한다: 공유(N개 에이전트 반영) / 에이전트(해당 에이전트만). AIA 보고 문장이 화면 어휘와 일치하게 된다.
- 기존 백업/복원 프롬프트(`skillTransfer.ts`)의 경로·규칙 산문 서술은 "get_skill_library를 호출해 스키마 기준으로 작업"으로 축소한다.

## 8. 안전 불변식

- 클라이언트(UI·AIA)는 **id/key만 넘긴다. 경로 인자는 받지 않는다.** 코어가 호출 시점 재스캔으로 id→경로를 해석하고 허용된 에이전트 루트 안인지 검증한다.
- 쓰기(편집·삭제)는 scope `personal`/`project`만 허용, `read_only` 루트는 코어에서 거부한다.
- 스킬 키 네임스페이스는 전역이다. 공유 스킬과 같은 키의 로컬 스킬은 그 배포본으로 취급된다(기존 divergence 감지와 같은 전제). 저장 전 반영 범위 고지가 이 케이스의 안전장치다.
- 검증 제약(키 패턴, 길이, 크기 한도)과 `schemaVersion`을 응답 데이터에 포함해 TS/Rust/AIA 3중 규칙 복제를 없앤다.
- 비밀값으로 보이는 파일은 편집기·AIA 응답에 원문 노출하지 않는다.

## 9. 구현 순서와 터치포인트

**1단계 — 휴지통 인프라 + 삭제 + 상태 해석 변경**
- core: 휴지통 모듈(이동/manifest/목록/복구/비우기), `delete_installed_skill`, `delete_shared_skill`(그룹), `lib.rs` export, `remote.rs` dispatch
- 상태 해석: `skillLibrary.ts`의 `defaultPublishTargets`·`skillSyncState`·라벨, Rust 대응 로직
- UI: `SkillsView.tsx` 삭제 버튼+확인 다이얼로그, `ipc.ts` 래퍼, i18n

**2단계 — 편집 + 공유 토글**
- core: `update_skill`(공유→원본+재배포 라우팅), `set_skill_shared`(ON=기존 import+publish 조합, OFF=사본 보존 전환), `set_skill_agent_deployment`
- UI: 파일 트리 기반 편집기(`SkillDetail` 재사용), 스킬공유 토글, 에이전트 매트릭스, 외부 수정 감지 해소 2버튼, 반영 범위 고지

**3단계 — AIA MCP 노출**
- `system_mcp.rs` `capability!` Read 4건 + Execute 5건, 용어 정렬, `skillTransfer.ts` 프롬프트 축소

**4단계 — 휴지통 UI**
- 목록/복구/비우기 화면, 용량 표시

각 단계는 코어 테스트(기존 `skill_library.rs` 테스트 관례)와 `skillLibrary.test.mjs` 갱신을 포함한다.

## 10. 범위 제외·후속

- 프로젝트 루트 배포는 현행대로 잠금 유지("어느 프로젝트에 게시할지 단일하게 정할 수 없다"). 향후 열더라도 키 기준 판별이라 규칙 변경 없음.
- 휴지통 자동 만료, 에이전트별 부분 분리(detach)는 도입하지 않는다.
- Antigravity는 사용자 스킬 루트가 생기면 어댑터 루트 추가만으로 배포 대상에 편입된다.
- 화면 두 곳(스킬 메뉴/라이브러리 패널)의 물리적 통합 범위는 1단계에서 어휘·배지 교체를 먼저 하고, 6절 구조로의 개편은 2단계에서 수행한다.
