describe("채팅 실행설정 공급자별 선택지", () => {
  beforeEach(() => {
    cy.visitApp();
    // 세션 뷰 열기
    cy.openView("sessions");
    // 새 세션 생성 버튼 클릭 (세션이 비어 있으므로 새 세션 버튼이 있거나 즉시 대시보드에서 생성할 수 있음)
    // 실제 UI에 맞춰 앵커를 찾자.
  });

  it("Codex가 아닌 공급자는 자동 검토가 비활성화된다", () => {
    // E2E 격리 환경에서는 공급자가 무엇인지?
  });
});
