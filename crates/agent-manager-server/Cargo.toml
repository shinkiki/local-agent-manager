[package]
name = "agent-manager-server"
version = "0.2.0"
description = "Loopback-only HTTP adapter for Agent Manager remote access"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"
repository = "https://github.com/shinkiki/local-agent-manager"

[dependencies]
agent-manager-core = { path = "../agent-manager-core" }
